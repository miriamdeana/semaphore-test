<% if names_found.empty? %>
  <b>No users with <%= formatted_caller_number %></b>
<% else %>
    <b>Users with <%= formatted_caller_number %></b>
  <ul>
    <% names_found.each do |i| %>
      <li><%= i %></li>
    <% end %>
  </ul>
<% end %>